# Configuration file linting the yaml files that configure Azure Pipelines
# This file is *not* applied to any pipelines consuming the yaml linting template.
---
extends: default

rules:
  # Too many things break when this is enabled, due to how nested things can be
  line-length: disable

ignore: /.git/
